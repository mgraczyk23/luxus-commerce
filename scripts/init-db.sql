-- Runs on FIRST PostgreSQL start only.
-- Medusa database (luxus_medusa) created automatically via POSTGRES_DB.
-- This creates the Payload CMS database.
CREATE DATABASE luxus_payload;
GRANT ALL PRIVILEGES ON DATABASE luxus_payload TO luxus_admin;
